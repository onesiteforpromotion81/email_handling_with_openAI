# Copy to .env and fill in your key.
# Get a key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional: override default model (must support JSON mode well)
# OPENAI_MODEL=gpt-4o-mini

# Optional: sampling temperature for triage (0–2)
# LLM_TEMPERATURE=0.2
